name: astean1001/solidity-queries
version: 0.0.1-dev
groups:
  - solidity
  - queries
suites: codeql-suites
extractor: go
defaultSuiteFile: codeql-suites/solidity-code-scanning.qls
dependencies:
  astean1001/solidity-all: ${workspace}
  codeql/suite-helpers: ${workspace}
  codeql/util: ${workspace}
warnOnImplicitThis: true